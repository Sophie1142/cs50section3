#include <stdio.h>

// Write a recursive function search that takes in four inputs,
// a value to search for, a sorted array of values, a start index,
// and an end index. The function should perform binary search,
// returning true if the number is found between the start and end index,
// and false otherwise.

bool search(int value, int values[], int start, int end)
{
    // TODO
}